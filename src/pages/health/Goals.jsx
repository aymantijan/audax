import GoalsBoard from './goals/GoalsBoard';

// Santé > Progrès > Objectifs — every goal of the unified system (including
// the ones linked to a Programme, badged as such).
export default function Goals() {
  return <GoalsBoard />;
}
